# =============================================================================
# AI 가계부 자동화 - 환경변수 설정 예시
# =============================================================================
# 이 파일을 복사하여 .env.local 파일을 만들고 실제 값으로 변경하세요.
# 
# 설정 방법:
# 1. 이 파일을 .env.local로 복사
# 2. 아래 값들을 실제 값으로 변경
# 3. Git에 .env.local은 커밋하지 마세요 (보안상 중요!)
#
# =============================================================================

# -----------------------------------------------------------------------------
# 🔑 Google Cloud & Sheets API 설정 (필수)
# -----------------------------------------------------------------------------
# Google Cloud Console에서 서비스 계정을 생성하고 JSON 키를 다운로드한 후
# 해당 JSON 파일의 값들을 아래에 입력하세요.
#
# 📋 설정 단계:
# 1. Google Cloud Console (https://console.cloud.google.com) 접속
# 2. 새 프로젝트 생성 또는 기존 프로젝트 선택
# 3. Google Sheets API 활성화
# 4. Google Drive API 활성화 (선택사항, 권한 문제 해결용)
# 5. 서비스 계정 생성 및 JSON 키 다운로드
# 6. 아래 값들을 JSON 키 파일에서 복사

# Google Cloud 프로젝트 ID
GOOGLE_PROJECT_ID=your-project-id-here

# 서비스 계정 Private Key ID
GOOGLE_PRIVATE_KEY_ID=your-private-key-id-here

# 서비스 계정 Private Key (따옴표 안에 전체 키를 입력하세요)
# 주의: \n은 실제 줄바꿈으로 자동 변환됩니다
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n"

# 서비스 계정 이메일 주소
GOOGLE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# 서비스 계정 Client ID
GOOGLE_CLIENT_ID=your-client-id-here

# -----------------------------------------------------------------------------
# 📊 Google Sheets 설정 (필수 - 둘 중 하나만 설정)
# -----------------------------------------------------------------------------
# 가계부 데이터를 저장할 Google Sheets의 ID를 입력하세요.
# 
# 📋 Google Sheets ID 찾는 방법:
# Google Sheets URL에서 추출: 
# https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit#gid=0
#                                    ↑ 이 부분이 SHEET_ID입니다
#
# ⚠️  중요: Google Sheets를 서비스 계정 이메일과 공유해야 합니다!
# 1. Google Sheets 열기
# 2. 우측 상단 "공유" 버튼 클릭
# 3. 서비스 계정 이메일 주소 입력
# 4. "편집자" 권한으로 설정
# 5. "무시하고 공유" 클릭 (외부 계정 경고 시)

# 방법 1: GOOGLE_SHEET_ID 사용 (권장)
GOOGLE_SHEET_ID=your-google-sheet-id-here

# 방법 2: GOOGLE_SHEETS_SPREADSHEET_ID 사용 (대안)
# GOOGLE_SHEETS_SPREADSHEET_ID=your-google-sheet-id-here

# -----------------------------------------------------------------------------
# 🤖 OpenAI API 설정 (선택사항)
# -----------------------------------------------------------------------------
# 현재는 사용하지 않지만, 향후 AI 기능 확장을 위해 준비
# OpenAI API 키가 있다면 설정하세요.
#
# 📋 OpenAI API 키 발급:
# 1. https://platform.openai.com/api-keys 접속
# 2. "Create new secret key" 클릭
# 3. 생성된 키를 아래에 입력

# OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# 🌐 Next.js 설정 (선택사항)
# -----------------------------------------------------------------------------
# Next.js 관련 환경변수들

# 개발/프로덕션 환경 구분
# NODE_ENV=development

# 애플리케이션 URL (배포 시 실제 도메인으로 변경)
# NEXTAUTH_URL=http://localhost:3000

# 보안을 위한 시크릿 키 (프로덕션에서는 반드시 변경)
# NEXTAUTH_SECRET=your-nextauth-secret-here

# -----------------------------------------------------------------------------
# 📝 추가 설정 참고사항
# -----------------------------------------------------------------------------
#
# 🔒 보안 주의사항:
# - .env.local 파일은 절대 Git에 커밋하지 마세요
# - 프로덕션 환경에서는 환경변수를 안전하게 관리하세요
# - API 키들은 정기적으로 교체하는 것이 좋습니다
#
# 🚀 배포 시 설정:
# - Vercel: 프로젝트 설정 > Environment Variables에서 설정
# - Netlify: Site settings > Environment variables에서 설정
# - 기타 플랫폼: 해당 플랫폼의 환경변수 설정 방법 참조
#
# 🐛 문제 해결:
# - Google Sheets API 403 오류: 서비스 계정과 시트 공유 확인
# - 환경변수 인식 안됨: .env.local 파일명과 위치 확인
# - Private Key 오류: 따옴표와 줄바꿈 문자 확인
#
# 📚 더 자세한 설정 방법은 README.md 파일을 참조하세요.
#
# =============================================================================
